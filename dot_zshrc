export ZSH="$HOME/.oh-my-zsh"
ZSH_THEME="robbyrussell"

ZSH_TMUX_AUTOSTART=true

plugins=(git tmux mise)

source $ZSH/oh-my-zsh.sh

export PATH=$PATH:~/.bin

source ~/.config/zsh/chezmoi-aliases.zsh

for f in ~/.zshrc.*; do
	[[ -f "$f" ]] && source "$f"
done
